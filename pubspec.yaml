name: jaep_movies_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  animate_do: ^5.1.0
  card_swiper: ^3.0.1
  dio: ^5.9.2
  flutter_dotenv: ^6.0.1
  flutter_lints: ^6.0.0
  flutter_riverpod: ^3.3.1
  flutter_test:
    sdk: flutter
  go_router: ^17.3.0
  youtube_player_flutter: ^10.0.1

flutter:
  uses-material-design: true
